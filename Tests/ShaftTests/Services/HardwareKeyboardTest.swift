import Shaft
import XCTest

class HardwareKeyboardTest: XCTestCase {
    func testEqual() {
        testWidgets { tester in

            // let keyboard = HardwareKeyboard.shared
            // tester.backend.onKeyEvent()
        }
        XCTAssertEqual(1 + 1, 2)
    }
}
